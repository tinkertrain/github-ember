<h1>GitHub Orgs & Repos</h1>

<section>
  <h4>Search</h4>
  <p>Type the name of an org and press <kbd>Enter ↩︎</kbd></p>
    <label for="user-org">Org name</label>
    {{input id="user-org" name="user-org" enter="handleEnter"}}
  {{outlet}}
</section>

<section>
  <h4>Here's a list of some famous orgs on GitHub</h4>
  <ul>
    {{#each model as |o|}}
      {{github-org org=o}}
    {{/each}}
  </ul>
</section>
